/**
 * Types for `rem.js`, which is CommonJS so `metro.config.js` can require it.
 * See that file for why the value is shared rather than written in two places.
 */
export declare const INLINE_REM: number;
